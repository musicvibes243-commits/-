# 21st.dev API key — get one at https://21st.dev/mcp
# Used by the 21st CLI (`--api-key $API_KEY_21ST`, or read from the env directly)
# and by the "21st" MCP server declared in .mcp.json.
# Copy to .env and fill in. Never commit the real key.
API_KEY_21ST=
